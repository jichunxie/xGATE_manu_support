# Python dependencies for the xGATE manuscript-support (reproducibility) repo.
# Versions are the ones used for the manuscript; loosened to minor ranges. For an
# exact snapshot use the conda env instead: envs/xgate.yml.
#
# ---------------------------------------------------------------------------
# The xGATE METHOD lives in a SEPARATE repository: https://github.com/jichunxie/xGATE
# This repo (xGATE_manu_support) only reproduces the manuscript results and
# DEPENDS ON that package, installed pinned to the manuscript release tag (below).
# Scripts and notebooks import it as `from xGATE.utilities import ...` /
# `import xGATE.utilities`.
#
# NOTE: the @v1.0 pin requires the xGATE repo to be PUBLIC and TAGGED v1.0. For an
#   archival-exact build, replace @v1.0 with the release commit hash.
#   (see docs/xgate_dependency_plan.md)
# ---------------------------------------------------------------------------

# xGATE method package (manuscript release):
git+https://github.com/jichunxie/xGATE.git@v1.0

# Pinned to the verified `xgate` conda env used for the manuscript (envs/xgate.yml),
# Python 3.10.20. For an exact snapshot prefer that conda env.
numpy==1.26.4
pandas==2.3.3
scipy==1.15.2
statsmodels==0.14.6
scikit-learn==1.7.2
networkx==3.4.2
python-igraph==1.0.0
matplotlib==3.10.9
seaborn==0.13.2
pillow==12.0.0        # build_assets.py composites PNG panels
scanpy==1.11.5
anndata==0.11.4
torch==2.12.0         # CPU build is sufficient
mygene==3.2.2
biopython==1.87
h5py==3.13.0
tables==3.10.1        # pytables (h5ad / HDF5 I/O)
# NOTE: the following are NOT present in the current `xgate` env snapshot.
#   PyYAML is only needed if you use configs/*.yaml (scripts fall back to the repo
#   root without it); jupyterlab/notebook are only needed to run the biology notebooks.
PyYAML>=6.0           # optional: config loading (configs/*.yaml)
jupyterlab>=4.0       # optional: to run the biology notebooks
notebook>=7.0         # optional
